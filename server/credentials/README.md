# Place your Google Cloud Service Account JSON here:
# google-service-account.json
#
# FREE setup:
# 1. console.cloud.google.com → create project
# 2. Enable "Google Sheets API"
# 3. Create Service Account → download JSON key
# 4. Rename/copy to: google-service-account.json (this folder)
# 5. Share your Google Sheet with the client_email inside the JSON (Viewer)
#
# Never commit the real JSON file (gitignored).
